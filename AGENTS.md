### Fast, vectorized and modular python reimplementation of Color-QBP

The old matlab code can be found under directory: `matlab_src` that needds to be ported over into fast, vectorized and modular python version. It will be a bonus task to re-write the entire pipeline in torch and make it differentiable.